# Lefthook configuration
# https://lefthook.dev/configuration/

commit-msg:
  commands:
    commitlint:
      run: npx commitlint --edit {1}

pre-commit:
  parallel: true
  commands:
    lint:
      glob: '*.{js,ts}'
      run: pnpm run lint
